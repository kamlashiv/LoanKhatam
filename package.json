{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node scripts/preinstall.js",
    "build": "npx pnpm install --ignore-scripts --no-frozen-lockfile && echo 'compilation skipped'",
    "start": "node index.js",
    "build:hostinger": "pnpm --filter @workspace/loan-tracker run build && powershell -Command \"Compress-Archive -Path artifacts/loan-tracker/dist/public/* -DestinationPath loankhatam-deploy.zip -Force\"",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "express": "^5.2.1",
    "@clerk/express": "^2.1.29"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
